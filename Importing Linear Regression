from sklearn.linear_model import LinearRegression
reg=LinearRegression()
reg.fit(X_train,y_train)

print("Train score:",reg.score(X_train,y_train).round(2))
print("Train score:",reg.score(X_test,y_test).round(2))
